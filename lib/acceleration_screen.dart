import 'package:flutter/material.dart';

class AccelerationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Acceleration Screen'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de aceleración'),
      ),
    );
  }
}
